version: 2
jobs:
  build:
    macos:
      xcode: "10.2"
    steps:
      - checkout
      - run:
          name: Test Rates App
          command: xcodebuild test -enableCodeCoverage YES -project Rates.xcodeproj -scheme Rates -sdk iphonesimulator -destination 'platform=iOS Simulator,name=iPhone Xʀ' | xcpretty